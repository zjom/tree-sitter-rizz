================================================================================
quote
================================================================================

'foo

--------------------------------------------------------------------------------

(source
  (quote
    datum: (quoted_ident)))

================================================================================
quote of list
================================================================================

'(1 2 3)

--------------------------------------------------------------------------------

(source
  (quote
    datum: (quoted_list
      (int)
      (int)
      (int))))

================================================================================
quasiquote
================================================================================

`(1 ,x ,@xs)

--------------------------------------------------------------------------------

(source
  (quasiquote
    datum: (quasi_list
      (int)
      (unquote
        datum: (ident))
      (unquote_splice
        datum: (ident)))))

================================================================================
nested quasiquote and quote
================================================================================

`',x

--------------------------------------------------------------------------------

(source
  (quasiquote
    datum: (quote
      datum: (unquote
        datum: (ident)))))

================================================================================
quote propagates to nested data
================================================================================

'((a b) [c {d: e}])

--------------------------------------------------------------------------------

(source
  (quote
    datum: (quoted_list
      (quoted_list
        (quoted_ident)
        (quoted_ident))
      (quoted_array
        (quoted_ident)
        (quoted_map
          (quoted_map_entry
            key: (quoted_ident)
            value: (quoted_ident)))))))

================================================================================
quasiquote escapes back to code via unquote
================================================================================

`(a ,(f b) c)

--------------------------------------------------------------------------------

(source
  (quasiquote
    datum: (quasi_list
      (quasi_ident)
      (unquote
        datum: (list
          (ident)
          (ident)))
      (quasi_ident))))
